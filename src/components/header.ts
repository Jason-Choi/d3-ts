import { html } from "lit";

const template = () => html`
  <nav class="navbar bg-light">
    <div class="container-fluid">
      <span class="navbar-brand mb-0 h1">Visual Analytics</span>
    </div>
  </nav>
`;

export default template;
